import styler from "../utils/styler";

const Footer = () => {
  styler();
  return (
    <footer className="bg-blue">
      <h1 className="logo fs-1rem tc-[primary-500]">Footer</h1>
    </footer>
  );
};

export default Footer;
